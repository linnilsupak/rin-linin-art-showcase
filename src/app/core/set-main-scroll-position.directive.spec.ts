import { SetMainScrollPositionDirective } from './set-main-scroll-position.directive';

describe('SetMainScrollPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new SetMainScrollPositionDirective();
    expect(directive).toBeTruthy();
  });
});
