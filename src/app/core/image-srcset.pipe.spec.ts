import { ImageSrcsetPipe } from './image-srcset.pipe';

describe('ImageSrcsetPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSrcsetPipe();
    expect(pipe).toBeTruthy();
  });
});
