import { ScrollIntoViewWhenReachDirective } from './scroll-into-view-when-reach.directive';

describe('ScrollIntoViewWhenReachDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollIntoViewWhenReachDirective();
    expect(directive).toBeTruthy();
  });
});
