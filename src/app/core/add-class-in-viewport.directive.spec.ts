import { AddClassInViewportDirective } from './add-class-in-viewport.directive';

describe('AddClassInViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassInViewportDirective();
    expect(directive).toBeTruthy();
  });
});
