import { AddClassOnScrollPositionDirective } from './add-class-on-scroll-position.directive';

describe('AddClassOnScrollPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassOnScrollPositionDirective();
    expect(directive).toBeTruthy();
  });
});
