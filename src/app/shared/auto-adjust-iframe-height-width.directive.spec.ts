import { AutoAdjustIframeHeightWidthDirective } from './auto-adjust-iframe-height-width.directive';

describe('AutoAdjustIframeHeightWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoAdjustIframeHeightWidthDirective();
    expect(directive).toBeTruthy();
  });
});
